{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build current file",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build/debug --target ${fileBasenameNoExtension}",
            "group": "build",
            "problemMatcher": ["$gcc"]
        }
    ]
}
